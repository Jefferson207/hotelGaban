import { BedDouble, Bath, Tv, Wifi, Droplets, ArrowUpRight } from 'lucide-react';import { motion } from 'framer-motion';import { rooms, WHATSAPP_NUMBER } from '../data/hotelData';
const icons=[BedDouble,Bath,Tv,Wifi,Droplets];const ask=name=>{if(!WHATSAPP_NUMBER){alert('Configura WHATSAPP_NUMBER en src/data/hotelData.js para recibir reservas.');return}open(`https://wa.me/${WHATSAPP_NUMBER}?text=${encodeURIComponent(`Hola, quisiera consultar disponibilidad para ${name} en Hotel Plaza San Gaban.`)}`,'_blank')}
export default function Rooms(){return <section id="habitaciones" className="bg-[#e8e0d1] py-24 lg:py-32"><div className="mx-auto max-w-7xl px-6"><p className="eyebrow">DESCANSO A TU MANERA</p><h2 className="title">Encuentra tu espacio ideal</h2><div className="mt-12 grid gap-6 md:grid-cols-3">{rooms.map((room,i)=><motion.article whileHover={{y:-7}} transition={{duration:.25}} className="overflow-hidden bg-[#f8f5ef]" key={room.name}><img className="h-72 w-full object-cover" src={room.image} alt={room.name} loading="lazy"/><div className="p-6"><span className="text-[10px] font-bold tracking-[.2em] text-[#88945d]">0{i+1}</span><h3 className="mt-3 font-serif text-3xl">{room.name}</h3><p className="mt-2 text-sm text-[#607166]">{room.description}</p><ul className="mt-5 space-y-2 border-t border-[#d8d0c2] pt-4">{room.features.map((f,index)=>{const Icon=icons[index];return <li className="flex gap-2 text-xs text-[#4d6257]" key={f}><Icon size={15}/>{f}</li>})}</ul><button onClick={()=>ask(room.name)} className="mt-4 flex items-center gap-3 text-xs font-bold text-[#214237]">Consultar disponibilidad <ArrowUpRight size={16}/></button></div></motion.article>)}</div></div></section>}
