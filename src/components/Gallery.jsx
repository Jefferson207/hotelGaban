import { useState } from 'react';import { ImagePlus, X } from 'lucide-react';import { motion,AnimatePresence } from 'framer-motion';import { gallery,galleryAlt } from '../data/hotelData'
export default function Gallery(){const [images,setImages]=useState(gallery),[selected,setSelected]=useState(null);const upload=e=>{const added=Array.from(e.target.files||[]).filter(file=>file.type.startsWith('image/')).map(file=>URL.createObjectURL(file));setImages(current=>[...current,...added]);e.target.value=''};return <section id="galeria" className="bg-[#f6f3ed] py-24 lg:py-32"><div className="mx-auto max-w-7xl px-6"><div className="flex flex-wrap items-end justify-between gap-5"><div><p className="eyebrow">EXPERIENCIAS</p><h2 className="title">Momentos para recordar</h2><p className="copy max-w-lg">Paisajes, descanso y naturaleza para disfrutar San Gabán a tu ritmo.</p></div><label className="inline-flex cursor-pointer items-center gap-2 rounded-full border border-[#173d33] px-4 py-2.5 text-xs font-bold text-[#173d33] transition hover:bg-[#173d33] hover:text-white"><ImagePlus size={16}/> Agregar fotos<input type="file" accept="image/*" multiple className="sr-only" onChange={upload}/></label></div><div className="mt-10 grid grid-cols-2 gap-3 md:grid-cols-4 md:grid-rows-2">{images.map((src,i)=><button onClick={()=>setSelected(i)} key={`${src}-${i}`} className={`overflow-hidden ${i===0?'col-span-2 row-span-2':i===3?'row-span-2':''}`}><img className="h-full min-h-40 w-full object-cover transition duration-500 hover:scale-105" src={src} alt={galleryAlt[i]||`Experiencia ${i+1}`} loading="lazy"/></button>)}</div></div><AnimatePresence>{selected!==null&&<motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={()=>setSelected(null)} className="fixed inset-0 z-50 grid place-items-center bg-[#041a14]/95 p-6"><button className="absolute right-5 top-5 text-white" aria-label="Cerrar"><X size={32}/></button><img onClick={e=>e.stopPropagation()} className="max-h-[85vh] max-w-[92vw] object-contain" src={images[selected]} alt={galleryAlt[selected]||`Experiencia ${selected+1}`}/></motion.div>}</AnimatePresence></section>}
