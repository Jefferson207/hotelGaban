import { useState } from 'react';import { X } from 'lucide-react';import { motion,AnimatePresence } from 'framer-motion';import { gallery,galleryAlt } from '../data/hotelData'
export default function Gallery(){const [selected,setSelected]=useState(null);return <section id="galeria" className="bg-[#f6f3ed] py-24 lg:py-32"><div className="mx-auto max-w-7xl px-6"><p className="eyebrow">MOMENTOS PARA RECORDAR</p><h2 className="title">Conoce nuestro entorno</h2><p className="copy max-w-lg">Paisajes que invitan a mirar más lento, respirar profundo y descubrir nuevas rutas.</p><div className="mt-10 grid grid-cols-2 gap-3 md:grid-cols-4 md:grid-rows-2">{gallery.map((src,i)=><button onClick={()=>setSelected(i)} key={src} className={`overflow-hidden ${i===0?'col-span-2 row-span-2':i===3?'row-span-2':''}`}><img className="h-full min-h-40 w-full object-cover transition duration-500 hover:scale-105" src={src} alt={galleryAlt[i]} loading="lazy"/></button>)}</div></div><AnimatePresence>{selected!==null&&<motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={()=>setSelected(null)} className="fixed inset-0 z-50 grid place-items-center bg-[#041a14]/95 p-6"><button className="absolute right-5 top-5 text-white" aria-label="Cerrar"><X size={32}/></button><img onClick={e=>e.stopPropagation()} className="max-h-[85vh] max-w-[92vw] object-contain" src={gallery[selected]} alt={galleryAlt[selected]}/></motion.div>}</AnimatePresence></section>}
